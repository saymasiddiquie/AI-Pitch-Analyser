# Core app
streamlit==1.31.0

# PDF parsing (use a single library to reduce conflicts)
pypdf==3.9.0
pdfminer.six==20221105  # fallback text extractor

# PPTX parsing (use latest 1.x, with matching wheels)
python-pptx==1.0.2
Pillow==11.0.0
lxml==5.2.2

# Gemini SDK + env loader
google-generativeai==0.3.1
python-dotenv==1.0.0

# rebuild 2025-09-21-12-10
